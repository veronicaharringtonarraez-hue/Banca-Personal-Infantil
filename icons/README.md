# Ícono de ChildBank

Aquí va el ícono de la app con el nombre exacto: **`childbank-512.png`**
(imagen PNG de 512×512).

Si falta, la app funciona igual pero al instalarla usará un ícono genérico.
Para ponerlo: sube aquí el archivo `childbank-512.png` (Add file → Upload files).
